
default_cron:
  service_enabled: yes
  service_state:   started
  shell:           /bin/bash                        # the shell to use for running cronjobs
  path:            /sbin:/bin:/usr/sbin:/usr/bin    # the path to set for running jobs
  mailto:          root                             # the address where mails should be sent to
